name: Pull Request auto-merge (eclipse-temurin-bot)

on:
  pull_request:
    types: [labeled]

permissions: {}

jobs:
  automerge:
    uses: adoptium/.github/.github/workflows/pr-auto-merge.yml@main
    with:
      allowed-author: eclipse-temurin-bot
